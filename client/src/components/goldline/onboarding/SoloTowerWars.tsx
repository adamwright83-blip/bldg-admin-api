import type{GoldlineOnboardingSession}from"@shared/goldlineOnboarding";import{compileTowerWarsArena}from"@shared/towerWarsArena";import"./onboarding.css";
export function SoloTowerWars({session,customerNames}:{session:GoldlineOnboardingSession;customerNames:{id:string;label:string;currentCents:number;priorCents:number|null}[]}){const arena=compileTowerWarsArena(customerNames);if(arena.mode==="ZERO_HOLDING")return <section className="gl-solo-wars"><h1>RAISE YOUR FIRST STRONGHOLD</h1><p>Complete legitimate first-world activity. Goldline will not conjure a business or customer to fill this arena.</p><a href="/driver">BEGIN FIRST MISSION</a></section>;if(arena.mode==="FOUNDING_SIEGE")return <section className="gl-solo-wars"><p>FOUNDING SIEGE · FANTASY PRESSURE</p><h1>{arena.holding.label}</h1><div className="gl-siege"><b>REAL HOLDING</b><span>⚔</span><b>{arena.enemy.label}</b></div><p>The Ruinbound are game fiction. They do not imply lost customers, decline, or competitor revenue.</p></section>;if(arena.mode==="GHOST_RIVALRY")return <section className="gl-solo-wars"><p>GHOST RIVALRY · REAL ECONOMICS</p><h1>{arena.holding.label}</h1><div className="gl-siege"><b>${(arena.holding.currentCents/100).toFixed(0)} CURRENT</b><span>VS</span><b>${(arena.ghost.cents/100).toFixed(0)} PRIOR GHOST</b></div></section>;return <section className="gl-solo-wars"><p>HOLDING RIVALRY</p><div className="gl-siege"><b>{arena.holdings[0].label}</b><span>VS</span><b>{arena.holdings[1].label}</b></div><p>Both sides are legitimate imported holdings. Revenue remains zero until economic evidence exists.</p></section>}
