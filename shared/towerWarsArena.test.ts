import{describe,it,expect}from"vitest";import{compileTowerWarsArena as c}from"./towerWarsArena";const h=(id:string,prior:number|null)=>({id,label:`Holding ${id}`,currentCents:100,priorCents:prior});describe("solo Tower Wars",()=>{it("fabricates no zero-state tower",()=>expect(c([]).mode).toBe("ZERO_HOLDING"));it("uses fiction for founding pressure",()=>expect(c([h("a",null)])).toMatchObject({mode:"FOUNDING_SIEGE",enemy:{kind:"fictional_entropy"}}));it("uses actual prior economics for ghost",()=>expect(c([h("a",90)])).toMatchObject({mode:"GHOST_RIVALRY",ghost:{cents:90}}));it("uses two legitimate holdings",()=>expect(c([h("b",null),h("a",null)])).toMatchObject({mode:"HOLDING_RIVALRY",holdings:[{id:"a"},{id:"b"}]}));});
