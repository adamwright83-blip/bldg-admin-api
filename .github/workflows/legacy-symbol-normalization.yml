name: Legacy symbol normalization

on:
  push:
    branches:
      - chatgpt/legacy-dayforge-semantic-hygiene-0924

permissions:
  contents: write

jobs:
  normalize:
    if: ${{ !contains(github.event.head_commit.message, '[legacy-symbols-normalized]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Normalize embedded retired symbols
        run: node scripts/normalize-embedded-retired-symbols.mjs
      - name: Commit normalization
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "Goldline Naming Migration"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --stat
          git commit -m "refactor: namespace embedded retired symbols [legacy-symbols-normalized]"
          git push origin HEAD:${GITHUB_REF_NAME}
