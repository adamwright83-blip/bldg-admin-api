name: Legacy prefix normalization

on:
  push:
    branches:
      - chatgpt/legacy-dayforge-semantic-hygiene-0924

permissions:
  contents: write

jobs:
  normalize:
    if: ${{ !contains(github.event.head_commit.message, '[legacy-prefixes-normalized]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/collapse-legacy-prefixes.mjs
      - shell: bash
        run: |
          set -euo pipefail
          git config user.name "Goldline Naming Migration"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --stat
          git commit -m "fix: collapse repeated legacy prefixes [legacy-prefixes-normalized]"
          git push origin HEAD:${GITHUB_REF_NAME}
