# GOLDLINE FIRST 2.5D CHAPTER — IMPLEMENTATION HANDOFF

SPOILERS: Adam deliberately does not read this file. Keep all player-facing updates neutral.
Read first-2_5d-chapter-execution-contract.txt and first-2_5d-chapter-continuity-contract.txt alongside this file. They contain the complete user authorization, acceptance gates, main/WIP push rules and scope. Do not ask Adam to explain again.

## 1. Current status
Starting origin/main and current working base: `6436f54258b809cb3412b8fc231dcd120c76cd05`. origin/main had not moved when Slice 1 completed and was pushed.
Worktree: `/Users/adamwrightpfi/Desktop/goldline-first-chapter`; branch `astra/first-2_5d-chapter-handoff` (WIP disaster-recovery commit `8d0bc8b`, pushed to origin).
Slice 1 COMPLETE and merged to main. All acceptance gates verified: 10/10 focused simulation tests pass, TypeScript baseline unchanged (pre-existing server/ errors only, none touching chapter files), production build succeeds, browser-driven graybox playthrough confirmed rendering/movement/combat/interact, and reload/interruption (pause-on-hidden, checkpoint resume mid-room) verified directly in a live preview.
Slice 2 COMPLETE. 15/15 focused tests pass (5 new: launch physics, redirect-to-bridge traversal, redirect-to-latch puzzle, redirect-to-confrontation combat stagger + direct-strike still viable, checkpoint persists mechanism state). TypeScript/build clean. Browser-verified rendering of launcher/redirector/weight in both Garden and Gallery.
Slice 3 PARTIAL/BLOCKED BY DESIGN — server contract implemented and unit-tested, migration WRITTEN BUT NOT APPLIED. Adam explicitly chose "write only, don't run" for the new `goldline_chapter_states` table: a schema migration against the real database is a hard-to-reverse shared-infrastructure change, so it needs his separate go-ahead to actually execute rather than being run autonomously mid-session. See section 12b. Client is NOT yet wired to call the new endpoint (the dev preview harness has no tRPC/QueryClient provider, and wiring it meaningfully only matters once a real device-switch is testable against a live table).
Slice 4 COMPLETE. The persistent choice point promised in the creative canon (section 2) is now implemented on top of the existing latch mechanism: Perrin's always-available manual handle vs. Inez's redirect-through-the-latch route are two physically different ways to open the same shortcut, and which one the player uses is remembered as `save.choice` ('preserve' | 'break') and changes actual gameplay afterward — Perrin's route grants a one-time saving grace against an otherwise-fatal hit (renewed each retry, consistent with an ongoing companion capability rather than a consumable), Inez's route buys a longer stagger window on the boss. 18/18 focused tests pass (3 new). TypeScript/build clean, browser-verified.
Slice 5 PARTIAL/BLOCKED BY DESIGN — same shape as Slice 3. Server contract (schema, migration file written but not applied, service, tRPC router reading the real `getTowerWarsToday().ledger`) is complete and unit-tested against fixture events only; per Adam's explicit "code + tests only, no live reads" instruction, nothing in this slice has executed against the real Tower Wars ledger or any database. See section 12d. Client is not wired (same reasons as Slice 3 — no live table, no real host yet).
Slice 6 COMPLETE (deterministic-brief tier only). `echoFollowUpService.ts`'s `buildEchoFollowUpBrief` is a pure function over the existing `commercial_follow_ups` record shape — it echoes the recorded due date, note, and assignee verbatim, and honestly flags what's missing rather than inventing it. No LLM call, no draft-editing UI, no send path — those weren't asked for and the "constrained editable draft" tier from the design doc is optional and explicitly deferred (see 12e). 7/7 new unit tests pass against fixture evidence, no live DB or LLM calls were made.
Slice 7 COMPLETE (branching logic; client wiring deferred like 3/5/6). `chapterOutcomeBranchService.ts` maps the existing `GoldlineVisitEvidence.outcome` truth classification ('follow_up'|'won'|'lost') onto a fictional branch: a real win opens the obvious route, a real rejection closes it but opens a genuinely different one (never treated as a win, never blocks chapter completion), and anything pending/unknown stays ambiguous rather than manufacturing optimism. `applyRealOutcome` makes a terminal outcome ('won'/'lost') sticky against a later, less-resolved signal. 8/8 new tests pass; pure logic, no DB/network calls needed or made. `realOutcome` is now reserved on the shared persisted contract.
Slice 8 COMPLETE. The brass inspection balcony (canon section 2's return secret) is visible-but-locked from the very first room, and unlocks the moment the chapter completes — completing the gallery encounter now returns the heroine to the Arrival Court (the locked ending beat: "return to court") instead of leaving her stranded past the finale, and the completion banner is dismissible ("Continue exploring") so the player can actually walk back and find it instead of being stuck on a modal. Reaching the balcony sets `secretSeen` once (idempotent — visiting again does nothing further). 20/20 focused tests pass (2 new). TypeScript/build clean, browser-verified in both the locked (dim, gated) and unlocked (lit, walkable) states.
Creative pass RECOVERED AND INTEGRATED (section 12j). A separate session generated and wired real production art (backgrounds, all three characters, mechanisms/props/FX), a dedicated presentation layer, contextual physical interaction cues, authored dialogue, a real host-adapter fix protecting server-supplied fields on save, and live wiring of the city-scale toggle plus the Slice 5/6 prepare/echo actions into the actual playable UI. Recovered from an uncommitted local state after that session hit its usage limit, verified, two remaining art gaps given safe visual fallbacks, one real UI bug (dialogue/city-note overlap) fixed. 190/190 tests, clean build, browser-verified across all three rooms and mobile.
The original checkout has unrelated untracked assets. Leave it alone. node_modules is a symlink to its installed dependencies, not a committed artifact.
Dev preview: `.claude/launch.json` config `goldline-first-chapter-preview` (vite.chapter-preview.config.ts, port 5191), gated development entry only — not wired into Driver/Admin routing yet.

## 2. Creative canon — SPOILERS ALLOWED HERE
Chapter id `the-last-valet`, title THE LAST VALET. Primary canonical building Century Park East. Fictional interiors expressly make no claim about real access or architecture. OPUS supplies an existing golf/redirect inspiration only, not a second location or fabricated partnership.
Three spaces: The Arrival Court (lower terrace), The Turntable Garden (middle machinery garden), The Departure Gallery (upper confrontation). They form one continuous upward service route. No additional kingdom.
Companion: Inez Vale, fictional restorer of impossible machines, dry humor, practical, refuses waste. Ability: hold a redirector in its selected detent temporarily, freeing the heroine to cross/use it. Chemistry is mutual admiration and understated flirtation; never tied to revenue.
Adversary: Bellwether, an immaculate mechanical maître d' who has scheduled the whole building's departure, including everyone in it. Wants a perfect departure with no loose ends. Comically courteous, attacks by dispatching wheeled brass luggage and snapping retractable queue ribbons. Not a real employee or customer. Starts with direct charges; later uses angled projectiles and recovery openings.
Supporting character: Perrin, fictional custodian of the departure gallery. Wants to preserve the ceremonial apparatus even at the cost of a slower escape. Offers the manual gallery route; Inez proposes redirecting a launched weight through its latch. Choice is remembered and changes assistance/ending, never business facts.
Opening: heroine arrives to find the departure mechanism running with nobody aboard. Inez is holding a lever with her boot: "If you are here for the view, it is leaving."
Beats: learn movement/dodge in arrival court; learn launch and redirect in garden; choose preserve apparatus versus break fictional latch; confront Bellwether; stop the departure; return to court.
Climax: Bellwether is vulnerable after a telegraphed dispatch. Direct dodge/strike approach always works. Launch/redirect can interrupt him and open a longer window.
Ending: departure stops, Inez releases the lever, Perrin reacts to the remembered decision. Inez: "Same place tomorrow? Preferably still attached to the building."
Return secret: a brass inspection balcony visible from the first court becomes reachable after finale unlocks the redirector's return detent. Contains a short optional character beat and view, not business-critical information.
Tone: bright luxurious adventure, precise absurdity, competence, flirtation without stats, brief authored lines. Do not reveal cast, machinery solutions, ending, secret, or event consequence in updates to Adam.

## 3. Gameplay design
Reuse Pixi; no renderer migration. Fixed oblique view, ground-plane collision and explicit height/layer illusion, not a full 3D physics engine. Keyboard WASD/arrows plus mobile direction pad; dodge Space; attack J; interact E. Touch buttons duplicate every action. One-device city/adventure toggle.
Movement: accelerate/decelerate using existing movementFeel helpers where suitable; fixed-step simulation, diagonal normalization, grounded shadows. Dodge has cooldown and brief invulnerability, no stamina economy. Attack has startup/recovery, short reach, one hit per swing. Hit-stop after contact, not after empty attacks. Preserve existing Clockhead and Expedition code.
Health is fictional encounter health only (3 hits). Death retries the last safe room checkpoint, restores enemy and transient shots; no business damage. Pause on hidden document or lost focus. Never simulate driving-time interaction.
LAUNCH: sends a fictional trolley/weight along a fixed lane at fixed speed. No arbitrary coins/ammo; cooldown is mechanical recovery.
REDIRECT: selects one of three stable headings (bridge, latch, confrontation). Intersecting launched weight takes that heading once per redirector, preserving legible speed. Same weight/redirect rules power bridge traversal, latch puzzle, and enemy stagger. Not two animation buttons.
Two approaches: direct dodge/strike (always available) or environmental stagger. Alternative manual traversal remains available regardless of any business event. Optional bank shot is a skill shortcut.

## 4. Desktop/mobile shared-state contract
Design contract for Slice 3, NOT implemented yet. chapterId `the-last-valet`; version 1.
Persist fiction only: {chapterId, version, revision, room: arrival|garden|gallery, checkpoint:{room,x,y}, mechanism:{heading:bridge|latch|confrontation,bridgeOpen,latchOpen}, encounter:idle|active|won, routes:string[], choice:null|preserve|break, restored:boolean, prepared:{armedAt:null|ISO,resolvedEventId:null|string}, secretSeen:boolean}.
Server derives tenant/player from existing auth, never request-supplied ownership. Store one row per tenant/player/chapter; compare revision on writes, dedupe request id. Safe checkpoints only, no mid-dodge animation persistence. On conflict return latest authoritative fictional state, client adopts and retries an intentional action only after validation. GET must not authorize business writes.
Desktop manipulates heading/armed state directly in scene; phone resumes exact heading and resulting route. Both allow perspective switch. No simultaneous device timing required. Frame loop, input, particles, hit-stop and projectile interpolation stay client transient. On reconnect fetch fiction and business event adapter; never reroll fiction or adopt stale checkpoint over newer server revision.

## 5. Real-business-event binding
Planned Slice 5: consume server-compiled TowerWarsBusinessEvent from existing towerWarsService canonical paid ledger. Exact entity `century_park_east`; source eventId and occurredAt retained. Only qualifying paid events admitted by existing canonical compiler; no inferred local paidAt or new payment integration. Require occurredAt after server armedAt. Transactional compare-and-set empty resolvedEventId plus unique event binding prevents duplication.
Prepare a fictional counterweight receiver. State WAITING is optional. Qualifying payment unlocks an upper firing position and stabilizes the balcony approach; it never kills boss or awards territory conquest. Wrong entity/type/pre-arm event ignored. Duplicate event returns same consequence. Offline means reconciliation on next load can apply consequence; do not claim background worker exists without implementing one. Keep event ingestion server-side, never accept browser-asserted revenue.
This binding DOES NOT write any order/payment/customer/business truth. Manual route and direct combat complete chapter with zero events. One binding only.

## 6. Echo workflow
Planned Slice 6: existing supported follow-up preparation. Resolve actual follow-up id and assigned mission through authorized server source; retrieve recorded history, exact promise/note, due date/channel and missing info. Evidence-linked deterministic brief always works. If existing drafting provider supports it, constrained editable draft uses only those facts; absence of provider gives brief and no fabricated prose.
Human chooses/edits/discards and performs contact. No autosend, calls, commitments, discounts or new outreach integrations. Baseline summaries/preparation never gated. Echo retains a reusable preparation preference for the same workflow, then automatically assembles future eligible context. Inez's translucent prior movement prepares the apparatus and stops at a human-required threshold. Show real prepared output in accessible field drawer. Acceptance must measure removed retrieval steps, not animation completion.
Existing actionRegistry FOLLOW_UP and authoritative follow-up record are intended source; inspect exact router/query before wiring, preserve assignment/tenant authorization.

## 7. Art direction
Only approved reference currently stored: `client/public/assets/goldline/chapters/the-last-valet/references/approved-art-direction.png`. Source generation exec-45c8988e-1d89-4c5a-b5c3-bd7c0c574481; preview approved by Adam in parent task. Reference only, NOT a flattened playable background. Dimensions to be recorded by inspection.
Bright ivory stone, teal/cyan water, California foliage, warm brass; fixed oblique view; legible shadows. Existing Trailblazer directional sprites may be reused in graybox. Finish art only in Slice 9 after gameplay is verified. No dark-mode chapter. No new generated art yet. Record all prompts in first-2_5d-chapter-art-prompts.md. Never show asset previews to Adam.

## 8. Scene composition
Shared design canvas 960x640, scale-to-fit camera on desktop/mobile; responsive controls outside canvas with large tap targets. Portrait can letterbox for graybox; final composition needs portrait-specific camera testing. Ground plane room bounds x=60..900,y=100..550. Hero start (150,470), exit near (830,160).
Arrival: ivory ground/back wall; foreground rail at lower edge; safe start; ribbon sentinel telegraphs before charging. Balcony visible upper-left but initially unreachable. Room exit after sentinel overcome; no required business action.
Garden: launcher left (220,380), redirector center (480,330), latch right (780,220), bridge upper-right; walkable perimeter/manual route so machinery not an event gate. Safe checkpoint lower-left. Mechanism interaction radius clearly marked in graybox; labels removed/replaced only when final art communicates purpose.
Gallery: Bellwether at (720,240), cover islands kept out of checkpoint, return exit lower-left after victory; Inez/Perrin stage at safe sides. Headings affect projectile route, not canonical geography. Foreground rails occlude feet appropriately. Room changes fade; resume always safe ground.

## 9. Code map
Created docs/goldline/HANDOFF-FIRST-2_5D-CHAPTER.md: authoritative human continuation.
Created docs/goldline/first-2_5d-chapter-handoff.json: machine summary.
Created docs/goldline/first-2_5d-chapter-{execution,continuity}-contract.txt: original user instructions, preserve.
Created client/public/assets/goldline/chapters/the-last-valet/references/approved-art-direction.png: approved style reference.
Implementation files will be listed here as created.

## 10. Integration map
Expedition movement/audio: REUSE suitable pure helpers, preserve existing runtime.
Existing encounter lifecycle: REUSED as business action boundary; new room combat stays fictional, not commercial mission status.
Campaign: DEFERRED wiring until chapter entry works; extend existing hosts rather than replace compiler.
Fiction assignment: REUSE stable identity principle; server chapter state extends persistence in Slice 3.
Forge: REUSE documented canonical valet/golf eligibility, EXTEND with two fixed behavior families in Slice 2.
World events / Tower Wars canonical ledger: REUSE in Slice 5; no new business-event writes.
Night Shift: NOT USED; no duplicate director or scheduler.
Driver/Admin: EXTEND with gated entry and same chapter component; no public unauthenticated operational route.
Echo/action registry/follow-up: REUSE in Slice 6; no outbound sender.
Siege: NOT REPLACED; preserve its own state and entry points.

## 11. Tests
`client/src/game/chapters/firstChapter/model.test.ts`: 10/10 passing. Covers acceleration-bounded movement, wall collision without dodge tunneling, diagonal normalization, pause short-circuit, attack reach/recovery/hit-stop/guard, facing-gated hits, dodge invulnerability + cooldown, death/retry checkpoint reset, interact proximity gating for switch and exit, and full three-room zero-business-event completion. TypeScript baseline (`npx tsc --noEmit`) unchanged from main — all remaining errors are pre-existing server/ issues unrelated to chapter files. Production build via `vite.chapter-preview.config.ts` succeeds (only a benign Pixi chunk-size warning). Browser-driven verification: rendering confirmed in Arrival Court and Turntable Garden, checkpoint persists to localStorage key `goldline:chapter-dev:the-last-valet:v1`, and a full page reload correctly resumes mid-chapter (garden room, lever already thrown) — confirming interruption/reload behavior. Live keyboard-hold movement could not be driven from the automated browser pane because the pane runs the tab backgrounded (`document.hidden===true`), which correctly triggers the app's own pause-on-hidden gate — this is the feature working as designed, not a defect. Reducer tests already give exact numeric coverage of movement/collision that the backgrounded pane can't exercise live; a human play test remains the way to confirm feel.

## 12c. Slice 4 implementation notes (SPOILERS — companion/adversary/ally identities)
The choice point is the garden latch: `MANUAL_LATCH` (Perrin's handle, always available, no weight needed) vs firing the weight to the `latch` heading (Inez's proposal). First one used sets `save.choice` and it sticks (`if(!s.save.choice)` guards on both paths) — this is deliberately a single irreversible-feeling story choice even though the shortcut itself only needs to open once. `choice==='preserve'` sets `ChapterState.grace=true` in every `createChapter`/`retryChapter` call from that save (Perrin's assistance is remembered, not consumed across the whole playthrough) and cancels exactly one lethal hit per life by clamping hp to 1 and cueing `'saved'` (mapped to the `barrier_release` audio cue). `choice==='break'` shortens the boss's post-stagger recovery clock by 500ms when the confrontation heading resolves (`s.enemy.clock=-500` instead of `0`), a mechanical reward for Inez's riskier method. Inez's canon ability ("hold a redirector in its selected detent") is represented by the existing `save.heading` persistence — no separate mechanic was added for it since the state already behaves that way; noted here so a future agent doesn't duplicate it.

## 12d. Slice 5 what's built (non-spoiler, mechanical)
- `drizzle/schema.ts`: `goldlineChapterEventBindings` (tenantId/chapterId/buildingId unique key, `armedAt`, `resolvedEventId`, `resolvedAt`) — a single row per chapter+building, never storing any order/payment/customer field.
- `drizzle/0068_goldline_chapter_event_bindings.sql`: additive, written, **not applied**.
- `server/goldlineWorld/chapterEventBindingService.ts`: `armChapterEventBinding` (idempotent — arming twice keeps the original `armedAt`) and `reconcileChapterEventBinding` (pure w.r.t. the ledger: caller passes in already-fetched `TowerWarsBusinessEvent[]`, function never fetches events itself). Reconcile filters to the correct `buildingId`, requires `occurredAt` after `armedAt`, and consumes the earliest qualifying event exactly once via an `UPDATE ... WHERE resolvedEventId IS NULL` compare-and-set — safe to call repeatedly (including after the player was offline) and safe under a concurrent race (loser gets `already_resolved`, not an error). 8/8 unit tests pass against fixture events (armed-idempotent, not-yet-armed no-op, correct event applies once, wrong building ignored, pre-arm event ignored, duplicate event doesn't double-apply, offline-timing works identically, tenant isolation).
- `server/goldlineWorld/chapterEventBindingRouter.ts` (`get`/`arm`/`reconcile`) registered on `systemRouter` as `goldlineChapterEventBinding`. `reconcile` is the only place that calls the real `getTowerWarsToday()` — that call has never executed in this session (no live DB configured in this environment, and no request was made to invoke it against production).
- What the consequence actually is (which upper firing position / balcony stabilization) is intentionally left to the client wiring step, deferred with Slice 3's — both need a live table and a real host.

## 12e. Slice 6 what's built (non-spoiler, mechanical)
- `server/goldlineWorld/echoFollowUpService.ts`: `buildEchoFollowUpBrief(evidence)` — pure, takes a `FollowUpEvidence` (id/pipelineId/status/dueAt/note/assignedTo, the exact shape of one `commercial_follow_ups` row) and returns either `{available:false, reason:'no_eligible_follow_up'}` or an `available:true` brief carrying the verbatim `note`/`dueAt`/`assignedTo` plus a `missingInfo` array naming what's actually absent (no assignee, empty note) — never a fabricated fact. `getNextEligibleFollowUp` mirrors `driverGameWorldService.listDriverGameWorld`'s existing tenant+assignee+open-status+earliest-due query shape, narrowed to just `commercial_follow_ups` (no mission/account joins Echo doesn't need).
- `server/goldlineWorld/echoFollowUpRouter.ts` (`brief` query) registered on `systemRouter` as `goldlineEchoFollowUp`, gated by the same `dayforgePipelineProcedure` (`commercial_pipeline` entitlement, operator roles) that already guards `commercialPipelineRouter` — no new authorization surface.
- Deliberately NOT built yet: the LLM-assisted "constrained editable draft" tier, any send/contact path, and any client UI. The design doc calls the draft tier optional ("if existing drafting provider supports it"); the deterministic brief is the tier that's actually required and is what's implemented. Client wiring is deferred with Slices 3 and 5 — same reasons (no live host, and here also no invocation of `invokeLLM` against a real transcript/tenant was requested or performed).

## 12f. Integration Gate (inserted before Slice 9, per Adam's mid-run instruction)
Adam asked, mid-session, for the already-built Slices 3/5/6/7 contracts to be proven working together as ONE loop before spending remaining capacity on art — not deferred entirely to Slice 11 — and specifically required the proof to exercise the SAME production router/service boundaries a real host would call, not a parallel fake.

**PRODUCTION DATABASE ROUND-TRIP: NOT VERIFIED.** No live or local test MySQL is reachable in this sandbox (project memory already established Railway's database is private-network only, unreachable outside `railway run`). Applying `drizzle/0067`/`0068` and exercising them against real MySQL over a real authenticated HTTP request has not happened and needs Adam's separate approval.

**SAFE INTEGRATED CONTRACT/HOST TEST: VERIFIED**, at two layers:
- `server/goldlineWorld/chapterIntegration.test.ts` (4/4) calls the real service functions (`chapterStateService`, `chapterEventBindingService`, `echoFollowUpService`, `chapterOutcomeBranchService`) directly against one shared mocked backend store, proving the service-level contracts compose.
- `server/goldlineWorld/chapterHostIntegration.test.ts` (4/4) goes one layer further and calls the REAL tRPC routers via `.createCaller(ctx)` — `chapterStateRouter`, `chapterEventBindingRouter`, `echoFollowUpRouter` — the exact procedure/auth-middleware/service chain a real chapter host would invoke over HTTP. Only `getDb` and the real Tower Wars ledger fetch (`getTowerWarsToday`) are mocked; tenant/role/entitlement authorization (`resolveDayforgeMembership`, `hasDayforgeEntitlement`) is exercised for real, including a negative test confirming a caller without the `commercial_pipeline` entitlement is rejected with `FORBIDDEN` before ever reaching Echo's service.

Both suites together prove, against mocked persistence only:
- **A (Slice 3):** desktop's `save` creates revision 1 with a mechanism state → mobile's `get` reads that exact state/revision → mobile's `save` advances to revision 2 → desktop's `get` sees mobile's progress, not stale local state.
- **B (Slice 5):** `arm` the receiver → chapter confirmed still incomplete/playable with no event yet → `reconcile` a qualifying event applies the consequence exactly once, written into the SAME persisted chapter-state row (not a side channel) → a duplicate reconcile call does not re-apply it.
- **C (Slice 6):** Echo's `brief` procedure is reachable and its shape carries no send-related field, through the real entitlement-gated procedure.
- **D (Slice 7):** a real `'lost'` outcome branches to `alternate_route_open` (never `route_open`), gets persisted, and the chapter stays marked incomplete regardless.

What remains for Slice 11: apply the two migrations to a real database (Adam's approval required), and build the real provider-wrapped chapter host page/route that calls these same routers over actual HTTP from a real UI — no new app routing was added in this pass, to avoid touching shared app entry files without full-app regression coverage in this session.

## 12g. Slice 9 status: BLOCKED on image generation, everything else done
No image-generation tool was available in this Claude Code session. Per the execution contract's explicit fallback (section 21), rather than stopping: `client/src/game/chapters/firstChapter/artManifest.ts` declares the complete asset contract (every background/foreground/character-frame/mechanism-state/fx asset, exact path, purpose, dimensions); `FirstChapter.tsx` now tries to load every declared path via Pixi's `Assets.load` and falls back to the current vector graybox per-element on failure (verified in browser: with zero art files present, rendering is pixel-identical to before this change, no console errors — dropping a real file at any manifest path activates it immediately with no further code changes). `docs/goldline/first-2_5d-chapter-art-prompts.md` now has a full production-ready prompt for every asset, extending the already-approved style reference, all marked PENDING.
**FINAL ART ASSET GENERATION remains the one concrete blocker for a finished-looking Slice 9** — the hooks, contract, and prompts are all in place; a future session with image generation (or Adam's own generation) can fill in the manifest paths one at a time.

## 12h. Slice 10 QA pass — Definition of Complete (contract section 30), answered honestly
Full regression: 170/170 tests pass across `client/src/game/chapters`, `server/goldlineWorld`, `server/towerWars`, `server/commercialPipeline` — no failures, no chapter-caused regressions. Full-repo TypeScript baseline: 40 pre-existing errors, zero touching any chapter file (verified by grep across the complete `tsc --noEmit` output, not just a chapter-scoped run). Production build clean. Browser-verified: desktop rendering across all three rooms, mobile viewport (375×812) layout with large tap targets and readable HUD, reload/checkpoint resume, pause-on-hidden-document.

GAME: movement/dodge/combat/death-retry/three-connected-spaces/two-genuinely-different-mechanics/mechanic-interaction/traversal+puzzle+combat-uses/two-approaches/secret — ALL TRUE, browser- and test-verified across Slices 1/2/8.
CONTINUITY: desktop/mobile share persistent fiction, mechanism state and checkpoint cross devices, reload works — TRUE at the **server-contract level** (Slices 3's CAS logic, Integration Gate router tests); **NOT YET TRUE at the live-database/real-host level** — no migration applied, no real chapter host page exists yet (Slice 11).
CHARACTERS: companion/adversary recognizable and affect play, supporting character creates a persisting choice, no affection meter, no real-person fiction — ALL TRUE (Slice 4).
REALITY: one real-event binding exists with correct truth-class handling, exactly-once, offline-safe, chapter finishes without it — TRUE at the **server-contract level** (Slice 5, Integration Gate); **NOT YET TRUE live** (same DB/host gap as Continuity above).
ECHO: one real workflow automated, unsupported facts rejected, draft editable-in-spirit (deterministic brief), no autosend — TRUE for the deterministic-brief tier that's actually required (Slice 6); the optional LLM-draft tier was never required and is explicitly not built.
OUTCOMES: negative real outcome can move fiction, rejection stays rejection, unknown stays unknown — TRUE at the **service level** (Slice 7, Integration Gate); not yet wired to a live outcome feed (same DB/host gap).
ART: premium bright 2.5D, not dark mode, readable collision, dimensional layers, assets reproducible — **FALSE for finished art** (Slice 9 is generation-blocked, contract/hooks/prompts only); the temporary vector graybox IS bright, readable, and not dark-mode, which is all Slice 1-8 ever required.
SAFETY: no production-data mutation for QA (confirmed — this entire session touched zero live databases), pause-on-hidden-document works (browser-verified), no unsafe touch demands (mobile buttons are large and clearly labeled) — TRUE.
FUN: reason to keep playing after required business work ends (the secret/revisit, the persistent companion choice) — present in design and code; **an actual human fun-gate playtest has not happened** (that requires Adam himself, not something I can self-certify).

## 12i. Slice 11 status
A real, authenticated, provider-wrapped chapter host now exists in the actual app: `client/src/pages/GoldlineChapterHost.tsx`, routed at `/goldline-chapter`, gated by the existing `AdminAuthGate` and lazy-imported — the exact pattern `/goldline-effectiveness` already uses, not a new routing mechanism. It is intentionally unlinked from any nav, following that same precedent (`INTERNAL · GOLDLINE CHAPTER — THE LAST VALET` in its header, matching the internal-page convention).

The host does real work: on mount it queries `trpc.system.goldlineChapterState.get`, adopts server state into the same localStorage key `FirstChapter` already reads/writes if present, and then renders the existing `FirstChapter` component unmodified (reusing it rather than duplicating it). Every 4s it diffs localStorage against what it last pushed and calls `.save` with the tracked revision, adopting the server's `latest` state on a revision conflict instead of overwriting it. All of this degrades silently to local-only play when the server call fails — which is guaranteed to happen right now, because **the migration still has not been applied to any database** (drizzle/0067, unchanged from Slice 3). This wiring cannot be verified end-to-end in this session for two independent reasons: no live database, and no logged-in browser session available to this tool for exercising an `AdminAuthGate`-protected route.

Full-repo TypeScript baseline held at 40 pre-existing errors (zero new), full client `vite build` succeeded (new `GoldlineChapterHost` chunk built cleanly), and the existing admin-routing test (`adminLiveModel.test.ts`, 13/13) still passes.

STILL NOT DONE, and deliberately not attempted without Adam:
1. **Apply `drizzle/0067_goldline_chapter_states.sql` and `drizzle/0068_goldline_chapter_event_bindings.sql`** to a real database via `applyDayforgeReleaseMigrations` with `DAYFORGE_RELEASE_DB=1` — a hard-to-reverse production schema change that needs Adam's explicit go-ahead to execute, exactly as it did when first raised at Slice 3.
2. **Generate the actual art** from `first-2_5d-chapter-art-prompts.md` — no image-generation tool was available in this session (Slice 9).
3. **A real logged-in browser walkthrough of `/goldline-chapter`** once #1 is done, to replace this session's code-level verification with an actual live confirmation.
4. **The final, deliberately minimal, spoiler-free player-facing entry decision** (which real surface — Driver, Admin, or elsewhere — should link to this chapter, and what that link's copy says) is Adam's call, not mine to make unilaterally; `/goldline-chapter` is ready to be that destination once 1–3 are resolved.

## 12j. Creative pass recovery and art integration (2026-09-09)
A separate Codex session began the premium art/presentation pass and hit its usage limit before it could commit or push. Its local work (one commit, `37eb814`, plus untracked generated art and provenance docs) was recovered from the shared main checkout, verified against its own new test suite, and pushed to the handoff branch as a disaster-recovery commit (`3f9b076`) before any further changes were made, per standard recovery procedure.

**What Codex actually built (all verified true, not just claimed):**
- `ChapterScene.ts` — a new, fully separate Pixi presentation layer (previously `FirstChapter.tsx` drew everything with raw `Graphics` calls inline; now a dedicated depth-sorted scene class owns all sprites/atlases/labels, decoupled from simulation). Real generated art now renders: three visually distinct room backgrounds, all three fictional characters with genuine visual presence (not text-only), and a fully illustrated launcher/redirector/weight/latch/balcony mechanism set with distinct visual states.
- `presentation.ts` — replaces generic "INTERACT"/"USE" labels with contextual physical hints ("Release launcher", "Turn redirector", "Stop departure") and authored, speaker-attributed dialogue lines that change based on room, held companion choice, and combat state — this is what gives the cast a voice, not just a sprite.
- `persistence.ts` — **the host-adapter fix**: `chapterToServer`/`chapterFromServer` correctly round-trip the local save shape through the server's nested `GoldlineChapterFictionState` contract, and critically, `chapterToServer` preserves server-supplied fields (`prepared`, `realOutcome`, `restored`) that the local client has no opinion on, so an ordinary local save can never clobber a real-event consequence or outcome branch written by the server. Regression-proven by `creativePass.test.ts`'s "ordinary play never resets server-provided event or real-outcome fields" test.
- `model.ts` — raised machinery is now real cover: a committed enemy charge that would hit a wall obstacle staggers instead of passing through it or hitting the player blindly, and the gallery boss's tell/charge speed tightens in a second phase (hp≤3). Both are genuine gameplay/physical-legibility improvements, not just art.
- `FirstChapter.tsx` / `GoldlineChapterHost.tsx` — real integration, not separate demos: a "City scale" toggle lets the player switch to the overhead desktop-scale camera and tap a redirector to cycle its heading (Slice 3's shared desktop/mobile perspective, now actually reachable from the same component instead of only existing as a server contract); `onPrepare`/`onEcho` props wire directly to the real Slice 5 event-binding arm/reconcile calls and the real Slice 6 Echo brief, both rendered as an in-fiction "Prepare receiver" button and a "Trace the familiar motion" panel rather than existing only as unreachable endpoints.
- `creativePass.test.ts` — 7 new tests, all passing, covering the cover-stagger mechanic, boss phase-two timing, the host-adapter round-trip, the server-field-preservation fix, hint radius accuracy, and choice-driven dialogue.

**What I fixed on top of the recovery:** two elements referenced in `ChapterScene.ts` (`scenery` atlas keyed to `props/scenery-atlas.png`, and `actions` atlas keyed to `characters/heroine-actions.png`) were never generated before credits ran out. `heroine-actions` already had a safe fallback (plain directional idle sprite) and needed no fix. `scenery` did not: the raised-machinery-island fallback was a flat, jarring green rectangle that clashed badly against the new painted backgrounds, and the foreground occlusion rail and shortcut crate had no fallback at all (invisible collision — a real legibility bug). Fixed both: the island fallback is now a warm stone/brass bevel that blends into every room's palette, the crate got the same treatment, and the foreground rail got a subtle translucent occluding fallback strip. Browser-verified across all three rooms and at mobile viewport. Also fixed a real UI bug the pass introduced: the "City scale" note panel and the character dialogue bubble occupied the same screen position and rendered on top of each other when both were active — dialogue now hides while in city scale (narratively coherent too: you're not standing next to a companion while looking at the overhead view).
- `docs/goldline/creative-character-provenance.md` — written from scratch (Codex's session ended before it could write this one, unlike its mechanism/environment equivalents). Reconstructed by SHA-256-matching the three accepted atlases against Codex's local `~/.codex/generated_images` cache; the exact prompt text for these three specifically could not be recovered and is recorded as UNKNOWN rather than invented.
- `docs/goldline/first-2_5d-chapter-art-prompts.md` — rewritten to reflect reality: the old Slice-9 manifest (separate per-state `.webp` files, my own earlier design) is fully superseded by Codex's atlas-based approach and has been removed from code (`artManifest.ts` deleted — zero remaining references). The doc now points to the three real provenance docs for what's done, and gives exact, convention-matching prompts for the two still-missing atlases (`props/scenery-atlas.png`, `characters/heroine-actions.png`) — still BLOCKED, no image-generation tool available in this session either.

**Verified before pushing:** 190/190 tests (27 in the chapter's own suites, 163 unaffected regressions across goldlineWorld/towerWars/commercialPipeline/admin routing), full-repo TypeScript baseline unchanged at 40 pre-existing errors (zero touching any chapter file), full client production build clean, and browser verification of all three rooms, mobile viewport, the city-scale toggle, and the dialogue-fix — all with the real generated art in place.

## 12. Remaining work
Slices 1, 2, 4, 6, 7, 8, 10, and 11's host-wiring are done, the creative pass (12j) is recovered/integrated, and the Integration Gate above still holds. What remains needs Adam directly: approving the two migrations, a real device/browser playthrough against a live database, generating the two still-missing art atlases, and the final decision on where the player entry point lives. Slice 3's server contract is written (schema, migration file, service, tRPC router — see 12b) but the migration has NOT been applied to any database; Slice 11's host now genuinely calls it, so applying the migration is the single remaining step to make cross-device play, the event binding, and Echo all real rather than locally-degraded.

## 12b. Slice 3 what's built (non-spoiler, mechanical)
- `drizzle/schema.ts`: `goldlineChapterStates` table (tenantId/operatorId/chapterId unique key, `revision` int, `stateJson` json, `lastRequestId` for idempotent retry) — same optimistic-concurrency shape as `goldlineCampaignInstances`.
- `drizzle/0067_goldline_chapter_states.sql`: additive `CREATE TABLE IF NOT EXISTS`, written, **not executed against any database**.
- `shared/goldlineChapterState.ts`: the persisted fiction contract (`GoldlineChapterFictionState` zod schema) — matches section 4's shape, with `choice`/`restored`/`secretSeen` fields already reserved for Slices 4 and 8 so no further migration should be needed for those.
- `server/goldlineWorld/chapterStateService.ts`: `getChapterState`/`saveChapterState` with compare-and-set on `revision` and idempotent `requestId` dedupe; throws `ChapterStateRevisionConflictError` carrying the latest authoritative row on a stale write. 7/7 unit tests (mocked db) pass: create, round-trip, CAS bump, stale-write rejection, idempotent retry, tenant isolation, no-db-available fallback.
- `server/goldlineWorld/chapterStateRouter.ts` + registration in `server/_core/systemRouter.ts` as `goldlineChapterState`: `get` query and `save` mutation on `dayforgeTenantOperatorProcedure` (tenant/operator derived server-side from auth, never client-supplied).

## 12a. Slice 2 implementation notes (non-spoiler, mechanical)
LAUNCH/REDIRECT is one shared mechanism family (`model.ts`: `LAUNCHER`, `REDIRECTOR`, `HEADINGS`, `HEADING_TARGETS`, `Weight` type, `stepWeight`). Interacting near a room's launcher spawns a weight that travels at a fixed speed to that room's redirector; interacting near the redirector cycles the room's available headings (persisted in `save.heading`); when the weight reaches the redirector it re-targets toward the selected heading and resolves an effect on arrival. Garden headings: `bridge` (sets `gardenOpen`, opens the exit gate — same flag the original manual lever also sets, so the lever remains a business-independent manual alternative to the mechanism, satisfying "two viable approaches" for traversal too) and `latch` (sets `latchOpen`, which removes the `SHORTCUT_CRATE` collision blocker — puzzle use). Gallery heading: `confrontation` (aims the weight at the enemy's current position; on arrival forces `stage:'recover'` early — combat use, an alternative to the direct dodge/strike approach which remains fully viable on its own). All three headings share one `Weight` object/state machine — not three separate mechanics. No business imports anywhere in `model.ts`.

## 13. Do-not-redesign list
Keep chapter/cast/three spaces/two families above. No extra buildings-as-levels, fictional customer facts, mandatory business event, affection economy, full 3D, engine migration, autosend, or deliberately tedious first-run Echo. Do not edit older combat systems to simplify integration. User authorized main pushes only for genuinely complete slices; partial must go to named handoff branch. Never force push.

## 14. Current blockers / open questions
None established. Remote main movement during slice is a STOP condition per user; fetch before push. Local dependency symlink is for this worktree only. Creative decisions require no further Adam approval.
